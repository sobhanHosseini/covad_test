mvtec_root: /home/sobhan_hosseini/datasets/mvtec

sae_weights: /mnt/nvme1/sobhan_hosseini/covad_test/sae_training/sae_vitl14reg_C4096_k64.pt
normal_patches_tensor: /mnt/nvme1/sobhan_hosseini/covad_test/sae_training/mvtec_normal_patches_vitl14reg.pt
normal_patches_index: /mnt/nvme1/sobhan_hosseini/covad_test/sae_training/mvtec_patch_index_reg.pt

category: hazelnut
dino_model: dinov2_vitl14_reg       # must match model used during SAE training

patch_overlap_threshold: 0.3        # keep patch if >=30% of its pixels fall inside the defect mask
min_anomaly_patches_per_atom: 10    # atom must activate on at least this many anomaly patches
top_atoms_for_validation: 50
normal_patches_sample_size: 2000    # rows to sample from the precomputed normal tensor

vlm_model: gemma4:e4b
ollama_host: http://localhost:6000
top_patches_per_atom: 9             # 3x3 grid sent to VLM

output_dir: /mnt/nvme1/sobhan_hosseini/covad_test/mac/outputs
device: cuda
